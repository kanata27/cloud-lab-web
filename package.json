{
  "name": "kanata-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "build": "node tools/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "playwright test",
    "dev": "npm run build && wrangler dev --config wrangler.preview.jsonc --ip 127.0.0.1 --port 8787",
    "deploy:preview": "npm run build && wrangler deploy --config wrangler.preview.jsonc",
    "deploy:production": "npm run build && wrangler deploy --config wrangler.production.jsonc"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "wrangler": "4.131.1"
  }
}
